spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/micro_ecom_order
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
  application:
    name: order-service
server:
  port: 5001
